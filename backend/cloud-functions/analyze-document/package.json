{
  "name": "analyze-document",
  "version": "1.0.0",
  "description": "Cloud Function to analyze documents with Vertex AI",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "deploy": "gcloud functions deploy analyzeDocument --gen2 --runtime nodejs20 --trigger-topic document-analyze --entry-point analyzeDocument --region asia-south1 --set-env-vars GCP_PROJECT_ID=indialawai-prod --project indialawai-prod"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.1.0",
    "@google-cloud/functions-framework": "^3.5.1",
    "@google-cloud/vertexai": "^1.0.0",
    "firebase-admin": "^12.0.0"
  }
}
